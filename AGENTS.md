# Every Morning Fortune 開発ルール

このリポジトリでは、既存のクライアントサイド PWA を小さく安全に改善する。

## 絶対条件

- OpenAI APIを含む外部APIを一切使用しない。
- 従量課金、有料サービス、追加クレジットを使用しない。
- HTML、CSS、JavaScript、ブラウザ標準機能のみで動作させる。
- 完全なクライアントサイド PWA を維持する。
- GitHub Pages で公開できる相対パス構成を維持する。
- iPhone Safari を最優先し、最新 iOS 対応の旧型 iPhone にも配慮する。
- 外部ライブラリを安易に追加しない。
- 既存機能やデザインを壊さない。
- 変更ファイルを必要最小限にする。
- 大規模な作り直しを行わない。
- 不明点を推測で実装しない。仕様または既存コードから判断できない場合は確認する。

## Every Morning 共通仕様

- Morning Navy (`#0b1830`) を基調とする。
- 起動アニメーションは約2秒とし、最大2.8秒で終了する。
- 起動画面には354度リングと終端の柔らかな光を表示する。
- アプリ名は静かにフェードインさせる。
- 音を使用しない。
- 起動画面の表現は SVG、CSS、JavaScript のみで実装する。
- `prefers-reduced-motion` とセーフエリアに対応する。
- 軽量で保守しやすいコードを優先する。

## 開発・確認ルール

- 変更前に `index.html`、`styles.css`、`js/`、manifest、Service Worker、および参照データを確認する。
- 管理用Studioは公開リポジトリへ戻さず、Mac内の管理者専用環境で維持する。
- 公開画面が参照する `js/` 配下を正とし、直下の重複 JavaScript は参照関係を確認せず変更しない。
- 機能追加、不要なリファクタリング、外部依存の追加は行わない。
- URL とアセット参照はリポジトリのサブパス配信でも動作する相対パスを使う。
- Service Worker のキャッシュ対象を変更した場合はキャッシュ名も更新する。
- ユーザーデータは端末内に保持し、外部へ送信しない。
- 修正後は JavaScript と JSON の構文、HTML・manifest・Service Worker のローカル参照、主要画面の起動、オフライン用キャッシュを可能な範囲で確認する。
- iPhone Safari とホーム画面追加後の PWA の最終確認は実機で行う。
